# Firebase Configuration
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-service-account-email
FIREBASE_PRIVATE_KEY="your-private-key-with-newlines"

# For GitHub Actions workflow
FIREBASE_SERVICE_ACCOUNT_BASE64=base64_encoded_service_account_json_file
# Note: To generate this value:
# 1. Download your Firebase service account JSON file
# 2. Run: cat your-service-account.json | base64
# 3. Copy the output as the value for FIREBASE_SERVICE_ACCOUNT_BASE64

# GitHub Configuration
GITHUB_REPOSITORY=username/repo-name
GITHUB_TOKEN=github_pat_your_personal_access_token

# Note: The GITHUB_TOKEN needs the following permissions:
# - Repository permissions:
#   - Actions: Read and write
#   - Contents: Read and write
#   - Metadata: Read-only
#   - Workflows: Read and write 